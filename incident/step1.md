# Task 1

Users report that the application at **http://localhost:8080** is returning an
error.

```bash
curl -i http://localhost:8080
```{{exec}}

Investigate, find the root cause, and restore the service.

Press **Check** when the application returns a successful response.
